runtime: nodejs
env: flex
service: push-notification

manual_scaling:
  instances: 1

env_variables:
  MODE: production

beta_settings:
  cloud_sql_instances: sever-prod:us-central1:sqlt890
